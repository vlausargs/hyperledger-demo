services:
  ca-org1:
    image: hyperledger/fabric-ca:1.5.5
    container_name: ca-org1
    environment:
      - FABRIC_CA_HOME=/etc/hyperledger/fabric-ca-server
      - FABRIC_CA_SERVER_CA_NAME=ca-org1
      - FABRIC_CA_SERVER_CA_CERTFILE=/etc/hyperledger/fabric-ca-server-config/ca.ca.org1.example.com-cert.pem
      - FABRIC_CA_SERVER_CA_KEYFILE=/etc/hyperledger/fabric-ca-server-config/priv_sk
      - FABRIC_CA_SERVER_TLS_ENABLED=false
      - FABRIC_CA_SERVER_DB_TYPE=postgres
      - FABRIC_CA_SERVER_DB_DATASOURCE=host=postgres-org1 port=5432 user=postgres password=org1_postgres_password_change_this dbname=fabric_ca_org1 sslmode=disable
    ports:
      - "8054:7054"
    volumes:
      - ca-org1_hlf_data:/etc/hyperledger/fabric-ca-server
    networks:
      - fabric_network
    command: sh -c "fabric-ca-server start -b ca.org1.example.com-admin:ca.org1.example.com-adminpw -d"
    healthcheck:
      test: ["CMD", "sh", "-c", "nc -z localhost 7054 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512m
          cpus: '1.0'
        reservations:
          memory: 256m
          cpus: '0.5'

volumes:
  ca-org1_hlf_data:
    driver: local

networks:
  fabric_network:
    driver: bridge
    name: fabric_network
